{
  "buildCommand": "npx prisma generate && pnpm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/sync-balances",
      "schedule": "*/5 * * * *"
    }
  ]
}
